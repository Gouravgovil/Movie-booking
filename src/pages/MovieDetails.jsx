import React, { useEffect, useState } from "react";
import MovieDetail from "../components/MovieDetails"; // Adjust the path as needed

const MovieDetails = () => {
    return (
        <MovieDetail/>
        
    );
};

export default MovieDetails;